# userinfo

# User Information CRUD App (PHP + HTML + CSS + JS)

This is a simple User Information system built with **PHP**, **MySQL**, and a **vanilla HTML/JS** frontend.

## 🔧 Setup Instructions

1. Clone this repo:
   ```bash
   git clone https://github.com/pinu1990/userinfo
